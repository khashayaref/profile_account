# Account Django Projects

## First clone the project
```
cd 'project_name'
```

## Then create virtual environment

## Second run the following command to install all Python modules
```
pip install -r requirements.txt
```

## Third run this:
```
python manage.py runserver
```